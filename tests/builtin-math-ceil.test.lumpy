try { dumpln(math::ceil(NaN)); } else err { dumpln(err); }
try { dumpln(math::ceil(+Inf)); } else err { dumpln(err); }
try { dumpln(math::ceil(-Inf)); } else err { dumpln(err); }
dumpln(math::ceil(+123));
dumpln(math::ceil(-123));
dumpln(math::ceil(+123.456));
dumpln(math::ceil(-123.456));
dumpln(math::ceil(+123.654));
dumpln(math::ceil(-123.654));
################################################################################
# "cannot convert float NaN to integer"
# "cannot convert float infinity to integer"
# "cannot convert float infinity to integer"
# 123
# -123
# 124
# -123
# 124
# -123
