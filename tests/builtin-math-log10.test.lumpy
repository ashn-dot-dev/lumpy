dumpln(math::log10(1).fixed(3));
dumpln(math::log10(2).fixed(3));
dumpln(math::log10(8).fixed(3));
dumpln(math::log10(10).fixed(3));
try { math::log10(0); } else err { dumpln(string(err)); }
try { math::log10(-1); } else err { dumpln(string(err)); }
################################################################################
# 0
# 0.301
# 0.903
# 1
# "math domain error"
# "math domain error"
