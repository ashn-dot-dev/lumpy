dumpln(math::acosh(NaN));
dumpln(math::acosh(+Inf));
try { math::acosh(-Inf); } else err { dumpln(err); }
dumpln(math::acosh(1));
dumpln(math::acosh(1.543).fixed(1));
try { math::acosh(-1); } else err { dumpln(err); }
################################################################################
# NaN
# Inf
# "math domain error"
# 0
# 1
# "math domain error"
