try { dumpln(math::floor(NaN)); } else err { dumpln(err); }
try { dumpln(math::floor(+Inf)); } else err { dumpln(err); }
try { dumpln(math::floor(-Inf)); } else err { dumpln(err); }
dumpln(math::floor(+123));
dumpln(math::floor(-123));
dumpln(math::floor(+123.456));
dumpln(math::floor(-123.456));
dumpln(math::floor(+123.654));
dumpln(math::floor(-123.654));
################################################################################
# "cannot convert float NaN to integer"
# "cannot convert float infinity to integer"
# "cannot convert float infinity to integer"
# 123
# -123
# 123
# -124
# 123
# -124
