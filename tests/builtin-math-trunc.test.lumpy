try { dumpln(math::trunc(NaN)); } else err { dumpln(err); }
try { dumpln(math::trunc(+Inf)); } else err { dumpln(err); }
try { dumpln(math::trunc(-Inf)); } else err { dumpln(err); }
dumpln(math::trunc(+123));
dumpln(math::trunc(-123));
dumpln(math::trunc(+123.456));
dumpln(math::trunc(-123.456));
dumpln(math::trunc(+123.654));
dumpln(math::trunc(-123.654));
################################################################################
# "cannot convert float NaN to integer"
# "cannot convert float infinity to integer"
# "cannot convert float infinity to integer"
# 123
# -123
# 123
# -123
# 123
# -123
