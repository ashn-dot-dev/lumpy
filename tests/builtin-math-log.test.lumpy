dumpln(math::log(1).fixed(3));
dumpln(math::log(2).fixed(3));
dumpln(math::log(8).fixed(3));
dumpln(math::log(10).fixed(3));
try { math::log(0); } else err { dumpln(string(err)); }
try { math::log(-1); } else err { dumpln(string(err)); }
################################################################################
# 0
# 0.693
# 2.079
# 2.303
# "math domain error"
# "math domain error"
