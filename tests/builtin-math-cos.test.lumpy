dumpln(math::cos(NaN));
try { dumpln(math::cos(+Inf)); } else err { dumpln(err); }
try { dumpln(math::cos(-Inf)); } else err { dumpln(err); }
dumpln(math::cos(0).fixed(1));
dumpln(math::cos(math::pi/2).fixed(1));
dumpln(math::cos(math::pi).fixed(1));
dumpln(math::cos((3 * math::pi) / 2).fixed(1));
dumpln(math::cos(2 * math::pi).fixed(1));
################################################################################
# NaN
# "math domain error"
# "math domain error"
# 1
# 0
# -1
# -0
# 1
