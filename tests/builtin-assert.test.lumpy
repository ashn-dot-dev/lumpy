assert(true); println("assert(true) does nothing");
try { assert(false); } else err { println("assert(false) produces " + repr(err)); }
################################################################################
# assert(true) does nothing
# assert(false) produces "assertion failure"
