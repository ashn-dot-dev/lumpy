try { dumpln( null.*         ); } else err { dumpln(err); }
try { dumpln( true.*         ); } else err { dumpln(err); }
try { dumpln( 123.*          ); } else err { dumpln(err); }
try { dumpln( "foo".*        ); } else err { dumpln(err); }
try { dumpln( [].*           ); } else err { dumpln(err); }
try { dumpln( map{}.*        ); } else err { dumpln(err); }
try { dumpln( set{}.*        ); } else err { dumpln(err); }
try { dumpln( 123.&.*        ); } else err { dumpln(err); }
try { dumpln( function(){}.* ); } else err { dumpln(err); }
try { dumpln( println.*      ); } else err { dumpln(err); }
################################################################################
# "attempted dereference of non-reference type `null`"
# "attempted dereference of non-reference type `boolean`"
# "attempted dereference of non-reference type `number`"
# "attempted dereference of non-reference type `string`"
# "attempted dereference of non-reference type `vector`"
# "attempted dereference of non-reference type `map`"
# "attempted dereference of non-reference type `set`"
# 123
# "attempted dereference of non-reference type `function`"
# "attempted dereference of non-reference type `function`"
