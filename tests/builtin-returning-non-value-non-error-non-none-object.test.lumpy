let source =
```
class BuiltinFoo(Builtin):
    name = String("foo")

    def function(self, arguments):
        return 123

let_builtin(BASE_ENVIRONMENT.store, BuiltinFoo())
```;
extend(source);
dumpln(foo());
################################################################################
# external(123)
