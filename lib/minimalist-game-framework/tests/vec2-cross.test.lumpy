let vec2 = import("../vec2.lumpy");

dumpln(vec2::cross(vec2::init(0, 0), vec2::init(1, 1)));
dumpln(vec2::cross(vec2::init(1, 1), vec2::init(0, 0)));

print("\n");

dumpln(vec2::cross(vec2::init(1, 1), vec2::init(1, 1)));

print("\n");

dumpln(vec2::cross(vec2::init(3, 5), vec2::init(7, 9)));
dumpln(vec2::cross(vec2::init(-3, 5), vec2::init(7, 9)));
dumpln(vec2::cross(vec2::init(3, -5), vec2::init(7, 9)));
################################################################################
# 0
# 0
#
# 0
#
# -8
# -62
# 62
